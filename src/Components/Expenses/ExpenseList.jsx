import React from "react";
import ExpenseItem from "./ExpenseItem";

const ExpenseList = () => {
  return (
    <React.Fragment>
      <ExpenseItem />
    </React.Fragment>
  );
};

export default ExpenseList;
